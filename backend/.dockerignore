# 의존성 폴더 (컨테이너 내부에서 npm ci로 새로 설치하므로 복사할 필요 없음)
node_modules

# 빌드 로그 및 에러 로그
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 소스 코드 (빌드된 build 폴더만 있으면 되므로 원본 src는 제외하여 용량 최적화)
src

# 설정 파일 및 로컬 환경 변수
.env
.env.local
.git
.gitignore

# 테스트 폴더
tests
coverage